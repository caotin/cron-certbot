{
  "name": "cron-certbot",
  "version": "1.0.0",
  "description": "Automatically renew Let's Encrypt certificates using certbot",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["certbot", "ssl", "certificate", "renewal", "cron", "letsencrypt"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.6",
    "dotenv": "^16.3.1"
  }
} 